/// <reference types="react" />
import * as React from 'react';
import Panel from './Panel';
export interface GenericTabDescriptor {
    id: string;
    url?: string;
    panelID?: string;
    accessibilityLabel?: string;
}
export interface TabWithTitleDescriptor extends GenericTabDescriptor {
    title: string;
}
export interface TabWithContentDescriptor extends GenericTabDescriptor {
    content: string;
}
export declare type TabDescriptor = TabWithTitleDescriptor | TabWithContentDescriptor;
export interface Props {
    selected: number;
    tabs: TabDescriptor[];
    fitted?: boolean;
    children?: React.ReactNode;
    onSelect?(selectedTabIndex: number): void;
}
export interface State {
    disclosureWidth: number;
    tabWidths: number[];
    visibleTabs: number[];
    hiddenTabs: number[];
    containerWidth: number;
    showDisclosure: boolean;
    tabToFocus: number;
}
export default class Tabs extends React.PureComponent<Props, State> {
    static Panel: typeof Panel;
    state: State;
    componentWillReceiveProps(nextProps: Props): void;
    render(): JSX.Element;
    private handleKeyPress(event);
    private renderTabMarkup(tab, index);
    private handleFocus(event);
    private handleBlur(event);
    private handleDisclosureActivatorClick();
    private handleClose();
    private handleMeasurement(measurements);
    private handleTabClick(id);
}
export declare function getTabContent(tab: TabDescriptor): string;
