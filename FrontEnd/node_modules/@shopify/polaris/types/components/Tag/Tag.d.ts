/// <reference types="react" />
export interface Props {
    /** Content to display in the tag */
    children?: string;
    /** Callback when tag is removed */
    onRemove?(): void;
}
export default function Tag({children, onRemove}: Props): JSX.Element;
