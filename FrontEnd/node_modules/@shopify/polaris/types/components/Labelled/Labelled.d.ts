/// <reference types="react" />
import * as React from 'react';
import { Action } from '../../types';
import { Props as LabelProps, labelID } from '../Label';
export { Action, labelID };
export declare type Error = boolean | string;
export interface Props {
    id: LabelProps['id'];
    label: string;
    error?: Error;
    action?: Action;
    helpText?: React.ReactNode;
    children?: React.ReactNode;
    labelHidden?: boolean;
}
export default function Labelled({id, label, error, action, helpText, children, labelHidden, ...rest}: Props): JSX.Element;
export declare function errorID(id: string): string;
export declare function helpTextID(id: string): string;
